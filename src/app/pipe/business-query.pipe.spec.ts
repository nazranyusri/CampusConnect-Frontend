import { BusinessQueryPipe } from './business-query.pipe';

describe('BusinessQueryPipe', () => {
  it('create an instance', () => {
    const pipe = new BusinessQueryPipe();
    expect(pipe).toBeTruthy();
  });
});
