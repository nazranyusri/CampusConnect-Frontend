import { PostQueryPipe } from './post-query.pipe';

describe('PostQueryPipe', () => {
  it('create an instance', () => {
    const pipe = new PostQueryPipe();
    expect(pipe).toBeTruthy();
  });
});
