import { SurveyQueryPipe } from './survey-query.pipe';

describe('SurveyQueryPipe', () => {
  it('create an instance', () => {
    const pipe = new SurveyQueryPipe();
    expect(pipe).toBeTruthy();
  });
});
