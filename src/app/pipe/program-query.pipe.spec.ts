import { ProgramQueryPipe } from './program-query.pipe';

describe('ProgramQueryPipe', () => {
  it('create an instance', () => {
    const pipe = new ProgramQueryPipe();
    expect(pipe).toBeTruthy();
  });
});
